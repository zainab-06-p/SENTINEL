# ── ACIR Platform — Task 1 Requirements ──────────────────────────────────────
# Install:  pip install -r requirements_task1.txt
# After install, ALSO run:  python -m spacy download en_core_web_lg
# Python 3.10+ required. Tested on 3.11 / 3.13.
# Elasticsearch Docker:  docker run -d --name es-acir -p 9200:9200 -e "discovery.type=single-node" elasticsearch:8.13.0

# Core ML / anomaly detection
pyod>=1.1.3
scikit-learn>=1.4.0
numpy>=1.26.0
pandas>=2.2.0
tsfresh>=0.20.3

# Elasticsearch client — must be >=8.0,<9.0 to match server version 8.13
elasticsearch>=8.13.0,<9.0.0

# PII scrubbing
presidio-analyzer>=2.2.356
presidio-anonymizer>=2.2.356
spacy>=3.7.0

# API
fastapi>=0.111.0
uvicorn>=0.29.0
python-multipart>=0.0.9
httpx>=0.27.0

# Data / utilities
pydantic>=2.7.0
faker>=24.0.0

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.7
